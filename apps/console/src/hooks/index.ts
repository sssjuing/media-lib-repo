import { usePathname } from './usePathname';
import { useRefresh } from './useRefresh';

const Hooks = {
  usePathname,
  useRefresh,
};

export {
  //
  Hooks as default,
  usePathname,
  useRefresh,
};
