import { useRoute } from '../RouteProvider';
import { useAuth } from './useAuth';
import { usePageTitle } from './usePageTitle';

const Hooks = {
  useRoute,
  useAuth,
  usePageTitle,
};

export {
  Hooks as default,
  //
  useRoute,
  useAuth,
  usePageTitle,
};
